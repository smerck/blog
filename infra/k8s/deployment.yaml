apiVersion: apps/v1
kind: Deployment
metadata:
  name: blog
  labels:
    app: blog
spec:
  replicas: 1
  selector:
    matchLabels:
      app: blog
  template:
    metadata:
      labels:
        app: blog
    spec:
      containers:
      - name: blog
        image: ghost:latest
        imagePullPolicy: Always
        ports:
        - containerPort: 2368
        env:
        - name: url
          value: http://blog.smerc.io
        volumeMounts:
        - mountPath: /var/lib/ghost/content
          name: content2
      volumes:
      - name: content2
        persistentVolumeClaim:
          claimName: blog-content2


